import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kyvy.vietnihontea',
  appName: 'VietNihonTea',
  webDir: 'dist',
  server: {
    androidScheme: "https"
  }
};

export default config;
